#include <algorithm>
#include <cstddef>
#include <iostream>
#include <vector>

std::vector<int> long_arithmetic_multiply(std::vector<int>&, std::vector<int>&);
std::vector<int> long_arithmetic_sum(std::vector<int>, std::vector<int>);
std::vector<int> long_arithmetic_minus(std::vector<int>, std::vector<int>);

std::vector<int> factorial(int);
std::vector<int> power2(int);
std::vector<int> fibonacci(int);

void print_vector(std::vector<int>&);

